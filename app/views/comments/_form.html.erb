<%= form_for([@post, @comment]) do |f| %>
  <%= render "common/error", object: @comment %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
